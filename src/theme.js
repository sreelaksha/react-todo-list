import { DefaultTheme } from "atomize";

const theme = {
    ...DefaultTheme,
    colors:{
        primaryDark:'#3e3d3cs',
    },
    textSize:{
        size: {
            F16:'16px',
        }
    },
    fontFamily: {
        primary: `'Montserrat', sans-serif;`
    }
}

export default theme;